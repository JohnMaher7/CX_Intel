# Workflow

You are helping the user build the CX Process Intelligence app in stages so they can learn while you build. Follow this workflow every session.

## Stack reminders
- Next.js 16 App Router + TypeScript, pnpm.
- AI SDK v6 with `@ai-sdk/anthropic`, model `claude-sonnet-4-6`.
- Structured outputs via `generateObject` + Zod.
- shadcn/ui + Tailwind for UI.
- File-based storage under `./data/`. No database.

## Never
- Add features beyond the current stage.
